<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        XML-тема используется только как initial theme для MainActivity (пока Compose не смонтировал
        setContent). Реальная тема приложения — `MoneyKeeperTheme` в :core:ui (§1.10), она же читает
        ThemeMode из Settings (§9.10). Здесь минимальный Material3 no-action-bar.
    -->
    <style name="Theme.MoneyKeeper" parent="android:Theme.Material.Light.NoActionBar"/>
</resources>
